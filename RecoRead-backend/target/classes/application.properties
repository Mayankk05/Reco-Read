spring.application.name=Bookwise-backend
# Application Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/bookwise_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=data@base
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# JWT Configuration
app.jwt.secret=58f3384951f1dcaf66034128ad45cf55664c903c020cf72d2ed0e983a99b74c7
app.jwt.expiration=86400000
# JWT expiration time in milliseconds (24 hours = 24 * 60 * 60 * 1000 = 86400000)

# Google Books API Configuration
google.books.api.key=AIzaSyB6O8_mQTrls5KBVBAKNeVcnopJwLhE1Tc
google.books.api.url=https://www.googleapis.com/books/v1
# ? CHANGED: Gemini API Configuration (Replace OpenAI config)
gemini.api.url=${GEMINI_URL}
gemini.api.key=${GEMINI_KEY}


logging.level.com.bookwise.service.AISummaryService=DEBUG
spring.main.allow-bean-definition-overriding=true

# CORS Configuration
app.cors.allowed-origins=http://localhost:3000

# Logging Configuration
logging.level.com.bookwise=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Jackson Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC